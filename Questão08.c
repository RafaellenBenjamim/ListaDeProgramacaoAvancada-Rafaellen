Considerando a declaração

int mat[4], *p, x;

quais das seguintes expressões são válidas? Justifique

◦ p = mat + 1; //Válida, pois soma o endereço de mat[0]+1, assim p pega o endereço de mat[1]
◦ p = mat;` // Válida, P recebe o endereço de mat[0]
◦ p =`mat; // Inválida, a aspa está mal posicionada
◦ x = (*mat);` // Válida, x recebe o conteúdo de mat[0]
